import { ComingSoon } from "../_stub/ComingSoon";

export const metadata = { title: "Agent Analytics · baby-profound" };

export default function AgentAnalyticsPage() {
  return (
    <ComingSoon
      title="Agent Analytics"
      note="Captured from the tryprofound.com workspace — Brex doesn't have this enabled."
      referenceSlug="agent-analytics"
    />
  );
}
