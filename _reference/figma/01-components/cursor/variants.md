# Cursor — variants

| `type` | Use |
|---|---|
| `Default` | normal pointer (10×17) |
| `Text` | text-edit caret/I-beam (8×14) |
| `Open hand` | drag-grab hand (12×12) |
| `Pointing` | hover-link hand (12×13) |

All four are bitmap raster images. Wrapper is always 20×20.

**Skip for Phase 3** unless we need to render fake cursors in marketing screenshots.
